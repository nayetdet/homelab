vault-unseal-cronjob:
  schedule: '"*/5 * * * *"'

  vault:
    address: http://vault.vault.svc.cluster.local:8200
    shards:
      - "$(VAULT_UNSEAL_SHARDS)"

  envFrom:
    - secretRef:
        name: vault-unseal
