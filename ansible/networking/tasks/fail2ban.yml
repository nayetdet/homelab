---
- name: Install Fail2ban
  ansible.builtin.apt:
    name: fail2ban
    state: present
    update_cache: yes
    lock_timeout: 300

- name: Ensure Fail2ban Service Is Enabled and Running
  ansible.builtin.service:
    name: fail2ban
    state: started
    enabled: yes
