apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: gatekeeper
  namespace: argocd
spec:
  project: default
  source:
    repoURL: ghcr.io/nayetdet/charts
    chart: gatekeeper
    targetRevision: "*"
    helm:
      parameters:
        - name: secret.enabled
          value: "false"
        - name: externalSecret.enabled
          value: "true"
        - name: externalSecret.storeName
          value: vault
        - name: sftpgo.sftpd.enabled
          value: "true"
        - name: sftpgo.ui.ingress.enabled
          value: "true"
        - name: sftpgo.ui.ingress.hosts[0].host
          value: gatekeeper.home.arpa
  destination:
    server: https://kubernetes.default.svc
    namespace: gatekeeper
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
